<UserControl x:Class="GalacticFrontier.Views.GalaxyMapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:GalacticFrontier"
             Background="{StaticResource BgBrush}">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="3*"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>
    <Border Grid.Column="0" BorderBrush="#4D00F5FF" BorderThickness="0,0,1,0" ClipToBounds="True">
      <Canvas x:Name="MapCanvas" Background="#050510" MouseLeftButtonDown="MapCanvas_OnMouseDown"
              MouseMove="MapCanvas_OnMouseMove" MouseLeftButtonUp="MapCanvas_OnMouseUp">
        <Canvas x:Name="MapContent">
          <Canvas.RenderTransform>
            <TranslateTransform x:Name="MapPan"/>
          </Canvas.RenderTransform>
        </Canvas>
      </Canvas>
    </Border>
    <Border Grid.Column="1" Style="{StaticResource SciFiPanelBorder}" Margin="0">
      <StackPanel>
        <TextBlock Style="{StaticResource SciFiTitle}" Text="{Binding SelectedName}" FontSize="16"/>
        <TextBlock Margin="0,8,0,0" Style="{StaticResource SciFiLabel}" Text="Faction"/>
        <TextBlock Foreground="{StaticResource TextPrimaryBrush}" Text="{Binding SelectedFaction}"/>
        <TextBlock Margin="0,8,0,0" Style="{StaticResource SciFiLabel}" Text="Economy"/>
        <TextBlock Foreground="{StaticResource TextPrimaryBrush}" Text="{Binding SelectedEconomy}"/>
        <TextBlock Margin="0,8,0,0" Style="{StaticResource SciFiLabel}" Text="Threat"/>
        <TextBlock Foreground="{StaticResource WarnBrush}" Text="{Binding ThreatStars}"/>
        <TextBlock Margin="0,12,0,0" Foreground="{StaticResource TextMutedBrush}" Text="{Binding StatusMessage}" TextWrapping="Wrap"/>
        <Button Margin="0,16,0,6" Style="{StaticResource SciFiButton}" Content="{x:Static local:Strings.Travel}" Command="{Binding TravelCommand}"/>
        <Button Margin="0,6" Style="{StaticResource SciFiButton}" Content="{x:Static local:Strings.DockTrade}" Command="{Binding TradeCommand}"/>
        <Button Margin="0,6" Style="{StaticResource SciFiButton}" Content="{x:Static local:Strings.Scan}" Command="{Binding ScanCommand}"/>
        <Button Margin="0,6" Style="{StaticResource SciFiButton}" Content="{x:Static local:Strings.Engage}" Command="{Binding EngageCommand}"/>
        <Button Margin="0,16,0,0" Style="{StaticResource SciFiButton}" Content="UPGRADES" Command="{Binding UpgradesCommand}"/>
      </StackPanel>
    </Border>
    <Border Grid.Column="0" Grid.ColumnSpan="2" VerticalAlignment="Bottom" Background="#CC0D1A2E" Padding="12,8">
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
        <TextBlock Margin="12,0" Foreground="{StaticResource CyanBrush}" FontFamily="{StaticResource HudFont}" Text="{Binding HudCredits}"/>
        <TextBlock Margin="12,0" Foreground="{StaticResource BlueBrush}" FontFamily="{StaticResource HudFont}" Text="{Binding HudFuel}"/>
        <TextBlock Margin="12,0" Foreground="{StaticResource SuccessBrush}" FontFamily="{StaticResource HudFont}" Text="{Binding HudHull}"/>
        <TextBlock Margin="12,0" Foreground="{StaticResource TextPrimaryBrush}" FontFamily="{StaticResource HudFont}" Text="{Binding HudCargo}"/>
        <TextBlock Margin="12,0" Foreground="{StaticResource WarnBrush}" FontFamily="{StaticResource HudFont}" Text="{Binding HudWave}"/>
      </StackPanel>
    </Border>
  </Grid>
</UserControl>
