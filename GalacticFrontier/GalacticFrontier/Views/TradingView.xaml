<UserControl x:Class="GalacticFrontier.Views.TradingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:GalacticFrontier"
             Background="{StaticResource BgBrush}">
  <Grid Margin="16">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="2*"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>
    <Border Grid.Column="0" Style="{StaticResource SciFiPanelBorder}" Margin="0,0,8,0">
      <DockPanel>
        <TextBlock DockPanel.Dock="Top" Style="{StaticResource SciFiTitle}" Text="COMMODITY MARKET" FontSize="16" Margin="0,0,0,8"/>
        <ListView ItemsSource="{Binding Commodities}" SelectedItem="{Binding SelectedCommodity}"
                  Background="Transparent" BorderThickness="0">
          <ListView.View>
            <GridView>
              <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="140"/>
              <GridViewColumn Header="Buy" DisplayMemberBinding="{Binding BuyPrice}" Width="60"/>
              <GridViewColumn Header="Sell" DisplayMemberBinding="{Binding SellPrice}" Width="60"/>
              <GridViewColumn Header="Owned" DisplayMemberBinding="{Binding Owned}" Width="60"/>
              <GridViewColumn Header="Trend" DisplayMemberBinding="{Binding Trend}" Width="60"/>
            </GridView>
          </ListView.View>
        </ListView>
      </DockPanel>
    </Border>
    <Border Grid.Column="1" Style="{StaticResource SciFiPanelBorder}">
      <StackPanel>
        <TextBlock Text="{Binding CreditsText}" Foreground="{StaticResource CyanBrush}" FontFamily="{StaticResource HudFont}"/>
        <TextBlock Margin="0,12,0,4" Style="{StaticResource SciFiTitle}" Text="{Binding SelectedName}" FontSize="14"/>
        <TextBlock Text="{Binding SelectedDescription}" Foreground="{StaticResource TextMutedBrush}" TextWrapping="Wrap"/>
        <TextBlock Margin="0,12,0,4" Style="{StaticResource SciFiLabel}" Text="Quantity"/>
        <Slider Minimum="1" Maximum="{Binding MaxQuantity}" Value="{Binding Quantity}"/>
        <Button Margin="0,12,0,6" Style="{StaticResource SciFiButton}" Content="{x:Static local:Strings.Buy}" Command="{Binding BuyCommand}"/>
        <Button Style="{StaticResource SciFiButton}" Content="{x:Static local:Strings.Sell}" Command="{Binding SellCommand}"/>
        <TextBlock Margin="0,12,0,4" Style="{StaticResource SciFiLabel}" Text="Cargo"/>
        <ItemsControl ItemsSource="{Binding CargoItems}">
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <TextBlock Foreground="{StaticResource TextPrimaryBrush}" FontSize="11">
                <Run Text="{Binding Key, Mode=OneWay}"/><Run Text=": "/><Run Text="{Binding Value, Mode=OneWay}"/>
              </TextBlock>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
        <TextBlock Margin="0,12,0,0" Foreground="{StaticResource CyanBrush}" Text="{Binding RouteHint}"
                   Visibility="{Binding ShowRouteHint, Converter={StaticResource BoolToVis}}" TextWrapping="Wrap"/>
        <TextBlock Style="{StaticResource SciFiError}" Text="{Binding ErrorMessage}"/>
        <Button Margin="0,16,0,0" Style="{StaticResource SciFiButton}" Content="{x:Static local:Strings.Back}" Command="{Binding BackCommand}"/>
      </StackPanel>
    </Border>
  </Grid>
</UserControl>
