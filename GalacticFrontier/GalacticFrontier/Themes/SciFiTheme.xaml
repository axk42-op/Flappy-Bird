<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:conv="clr-namespace:GalacticFrontier.Converters">
  <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>
  <conv:HealthToColorConverter x:Key="HealthColor"/>
  <conv:RepToStringConverter x:Key="RepText"/>

  <Color x:Key="BgColor">#0A0A1A</Color>
  <Color x:Key="CyanColor">#00F5FF</Color>
  <Color x:Key="BlueColor">#1E90FF</Color>
  <Color x:Key="WarnColor">#FFAA00</Color>
  <Color x:Key="DangerColor">#FF3333</Color>
  <Color x:Key="SuccessColor">#00FF88</Color>
  <Color x:Key="TextPrimaryColor">#E0E8FF</Color>
  <Color x:Key="TextMutedColor">#6A8AAA</Color>
  <Color x:Key="PanelColor">#0D1A2E</Color>

  <SolidColorBrush x:Key="BgBrush" Color="{StaticResource BgColor}"/>
  <SolidColorBrush x:Key="CyanBrush" Color="{StaticResource CyanColor}"/>
  <SolidColorBrush x:Key="BlueBrush" Color="{StaticResource BlueColor}"/>
  <SolidColorBrush x:Key="WarnBrush" Color="{StaticResource WarnColor}"/>
  <SolidColorBrush x:Key="DangerBrush" Color="{StaticResource DangerColor}"/>
  <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}"/>
  <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}"/>
  <SolidColorBrush x:Key="TextMutedBrush" Color="{StaticResource TextMutedColor}"/>
  <SolidColorBrush x:Key="PanelBrush" Color="{StaticResource PanelColor}"/>

  <FontFamily x:Key="TitleFont">Courier New</FontFamily>
  <FontFamily x:Key="HudFont">Courier New</FontFamily>

  <Style x:Key="SciFiPanelBorder" TargetType="Border">
    <Setter Property="Background" Value="#E60D1A2E"/>
    <Setter Property="BorderBrush" Value="#4D00F5FF"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="CornerRadius" Value="4"/>
    <Setter Property="Padding" Value="16"/>
  </Style>

  <Style x:Key="SciFiTitle" TargetType="TextBlock">
    <Setter Property="FontFamily" Value="{StaticResource TitleFont}"/>
    <Setter Property="FontSize" Value="26"/>
    <Setter Property="FontWeight" Value="Bold"/>
    <Setter Property="Foreground" Value="{StaticResource CyanBrush}"/>
    <Setter Property="HorizontalAlignment" Value="Center"/>
  </Style>

  <Style x:Key="SciFiSubtitle" TargetType="TextBlock">
    <Setter Property="FontFamily" Value="{StaticResource HudFont}"/>
    <Setter Property="FontSize" Value="13"/>
    <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}"/>
    <Setter Property="HorizontalAlignment" Value="Center"/>
  </Style>

  <Style x:Key="SciFiLabel" TargetType="TextBlock">
    <Setter Property="FontFamily" Value="{StaticResource HudFont}"/>
    <Setter Property="FontSize" Value="12"/>
    <Setter Property="Foreground" Value="{StaticResource BlueBrush}"/>
  </Style>

  <Style x:Key="SciFiError" TargetType="TextBlock">
    <Setter Property="FontFamily" Value="{StaticResource HudFont}"/>
    <Setter Property="FontSize" Value="12"/>
    <Setter Property="Foreground" Value="{StaticResource DangerBrush}"/>
    <Setter Property="TextWrapping" Value="Wrap"/>
  </Style>

  <Style x:Key="SciFiTextBox" TargetType="TextBox">
    <Setter Property="FontFamily" Value="{StaticResource HudFont}"/>
    <Setter Property="FontSize" Value="14"/>
    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
    <Setter Property="CaretBrush" Value="{StaticResource CyanBrush}"/>
    <Setter Property="Background" Value="#CC0D1A2E"/>
    <Setter Property="BorderBrush" Value="{StaticResource BlueBrush}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Padding" Value="8,6"/>
  </Style>

  <Style x:Key="SciFiPasswordBox" TargetType="PasswordBox">
    <Setter Property="FontFamily" Value="{StaticResource HudFont}"/>
    <Setter Property="FontSize" Value="14"/>
    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
    <Setter Property="Background" Value="#CC0D1A2E"/>
    <Setter Property="BorderBrush" Value="{StaticResource BlueBrush}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Padding" Value="8,6"/>
  </Style>

  <Style x:Key="SciFiButton" TargetType="Button">
    <Setter Property="FontFamily" Value="{StaticResource HudFont}"/>
    <Setter Property="FontSize" Value="13"/>
    <Setter Property="FontWeight" Value="Bold"/>
    <Setter Property="Foreground" Value="{StaticResource CyanBrush}"/>
    <Setter Property="Background" Value="#CC0D1A2E"/>
    <Setter Property="BorderBrush" Value="{StaticResource CyanBrush}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Padding" Value="16,10"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="bd" Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="3" Padding="{TemplateBinding Padding}">
            <Border.Effect>
              <DropShadowEffect Color="#00F5FF" BlurRadius="8" ShadowDepth="0" Opacity="0"/>
            </Border.Effect>
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="bd" Property="Background" Value="#FF1A2D4A"/>
              <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource CyanBrush}"/>
              <Setter TargetName="bd" Property="Effect">
                <Setter.Value>
                  <DropShadowEffect Color="#00F5FF" BlurRadius="14" ShadowDepth="0" Opacity="0.7"/>
                </Setter.Value>
              </Setter>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="bd" Property="Background" Value="#FF002233"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Opacity" Value="0.45"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="SciFiProgressBar" TargetType="ProgressBar">
    <Setter Property="Height" Value="10"/>
    <Setter Property="Foreground" Value="{StaticResource CyanBrush}"/>
    <Setter Property="Background" Value="#660D1A2E"/>
    <Setter Property="BorderThickness" Value="0"/>
  </Style>
</ResourceDictionary>
